---
assessment_id: MA-20260809-NOCTURNE-005
assessment_date: 2026-08-09
subject: "Nocturne production-inert operations admin foundation"
trigger: release
depth: standard
version: "c1207954989317ead7c6eecf77091fd3cab82e83 / Sites version 5"
scope: "Public read-only Nocturne catalogue plus production-inert admin workflow source that remains unavailable in production"
baseline_ref: MA-20260808-NOCTURNE-004
supersedes: MA-20260808-NOCTURNE-004
stage_readiness: 1
overall_maturity: L2
evolution_capability: E4
confidence: high
decision_impact: release
evidence_refs:
  - "docs/releases/NOCTURNE-005-RELEASE-RECEIPT.md"
  - "docs/admin/ADMIN_OPERATIONS_PROBE.md"
  - "tests/admin-workflow.test.mjs"
  - "tests/rendered-html.test.mjs"
  - "commit c1207954989317ead7c6eecf77091fd3cab82e83"
next_gate: "Confirm a non-ChatGPT app-owned authentication path before adding D1-backed attendance writes or exposing any admin route"
---

# Nocturne Operations Foundation Maturity Assessment

## Evaluation context

This assessment covers the accepted D0 operations workflow probe and its
production release as an inert foundation. The public catalogue remains
read-only. Production requests to `/admin` return 404, and no database,
authentication, upload or production write path exists.

The target of the next stage is a narrowly controlled internal operations
alpha covering authentication, durable attendance state and deterministic
review. The current Sites authentication guidance only confirms ChatGPT-owned
identity flows, while the Human Owner requires public viewing without a
ChatGPT account. App-owned admin authentication therefore remains a release
blocker for the next stage.

`Version c120795 / Overall Maturity L2 within the public catalogue and inert admin foundation / Evolution Capability E4 / Confidence high`

## Overall conclusion

Sites version 5 is suitable for the existing public catalogue because the
public routes and supplied photographs are unchanged, the production admin
route fails closed, and the prior Sites version remains available for rollback.

The operations workflow is mature enough for D0 product validation. It is not
qualified for real staff use. Authentication, durable audit, data recovery,
concurrency and production writes have no implementation evidence.

## Decision impact

Release the accepted inert foundation to production while retaining Sites
version 4 as the immediate rollback boundary. Continue the next slice only in
an isolated development branch. Do not expose `/admin` or create D1-backed
writes until a supported non-ChatGPT authentication boundary is confirmed.

## Capability evidence

| Dimension | Level | Evidence status | Evidence | Main gap | Confidence | Uncertainty source | Upgrade condition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Purpose and boundary | L3 | [HUMAN-CONFIRMED] | D0 scope accepted on 2026-08-09; production route remains unavailable | Real attendance rules remain assumptions | high | Staff workflow has not been observed | Confirm submitter, reviewer, cut-off, cancellation and conflict rules |
| Concept and architecture | L3 | [REVIEWED] | Revision and publication state machines, fixed simulated identities and pointer recovery are separated | Authentication and persistence topology is unresolved | high | Platform support for app-owned auth is not yet confirmed | Freeze the supported identity boundary and D1 ownership model |
| Implementation quality | L3 | [TESTED] | Lint, build, seven automated tests and independent review passed | Admin implementation is intentionally session-only | high | No durable state exists | Add isolated D1 migrations, ownership checks and recovery tests |
| Validation and evidence | L3 | [TESTED] | Desktop, 390-pixel mobile, workflow transitions and production 404 were verified | No real staff task has been completed | high | Current evidence is simulated | Complete one editor and one reviewer task with development data |
| Deployment reliability | L3 | [OBSERVED] | Sites version 5 deployed; public routes returned 200; `/admin` returned 404; worker error log was empty | Rollback has not been exercised | high | Provider rollback behaviour remains unobserved | Perform a controlled Sites rollback drill when operationally justified |
| Security and risk governance | L2 | [TESTED] | Production admin fails closed and cannot be enabled by an environment flag | No supported app-owned identity or authorization implementation | high | Sites guidance only confirms ChatGPT-owned identity flows | Implement and independently review a supported non-ChatGPT admin gate |
| Feedback and evolution | L4 | [OBSERVED] | Human feedback, multi-model planning, independent review, two safety defects, corrections and regression tests formed a closed update loop | Operational feedback is absent | high | The loop is based on development evidence | Repeat the loop with staff attendance tasks and production-safe telemetry |

## Topology and critical path

Human Owner acceptance → validated commit → `main` fast-forward → production
build and tests → Sites version 5 → custom-domain HTTP checks → worker error
log check → release receipt.

The release chain is closed. The next-stage chain is blocked at identity:

staff identity → authorization → attendance command → D1 transaction →
review → public schedule projection → cancellation or rollback.

The weakest link is the unresolved non-ChatGPT identity and authorization
boundary for internal staff.

No later node should be promoted to production until the identity and
authorization boundary is supported and tested.

## Change quality, risk and debt

变更类型：结构优化、闭环补全、系统能力提升；依据：本次变更把已验收的后台流程、状态门禁、回滚与下架约束固化为生产失效关闭的代码和自动化测试，同时没有开放新的生产入口。

The additional source increases repository size and maintenance surface, while
runtime risk remains bounded because the production page fails closed before
rendering the admin client.

Remaining risks:

1. A future developer could weaken the production 404 before authentication is
   ready.
2. Attendance domain rules have not been confirmed against real operations.
3. App-owned authentication support on the current Sites platform is unresolved.
4. Durable audit, migrations, backup and recovery have no evidence.

## Next gate

Confirm the supported non-ChatGPT identity path, document the attendance
state machine and implement a D1-backed development slice with isolated data.
The exit evidence is one editor submission, one independent review, one public
schedule projection, one cancellation, one conflict rejection and one recovery
test without exposing customer information.
